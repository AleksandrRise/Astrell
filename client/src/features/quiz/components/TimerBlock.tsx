export default function TimerBlock() {

    // Classes
    const wrapperClasses = "w-82 h-19 bg-black border-1 border-white/20 rounded-3xl"
    const textClasses = ""

    return (
        <section className={wrapperClasses}>
            <span className={textClasses}></span>
        </section>
    )
}