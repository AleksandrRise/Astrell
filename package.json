{
  "devDependencies": {
    "concurrently": "^10.0.3"
  },
  "scripts": {
    "dev": "concurrently \"cd client && npm run dev\" \"cd python-service && flask run\"",
    "setup": "npm install && cd client && npm install && cd ../python-service && pip install -r requirements.txt",
    "build": "cd client && npm run build",
    "typecheck": "cd client && npm run typecheck"
  }
}
